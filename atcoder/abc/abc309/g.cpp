#include <bits/stdc++.h>
#define _ ios_base::sync_with_stdio(0);cin.tie(0);
#define endl '\n'
#define all(x) (x).begin(), (x).end()
#define rall(x) (x).rbegin(), (x).rend()
typedef long long ll;
using namespace std;

/*
	cada numero ou num sufixo ou num prefixo da permutacao
	contar o contrario parece mais facil:
	- na real nao eh nao.
	contando o contrario parece um pouco inclusao exclusao
	+ quantos tem pelo menos 1 P[i] - i < x
	- quantos tem pelo menos 2 P[i] - i < x
	+ quantos tem pelo menos 3 P[i] - i < x
*/

int solve() {
	int n, x; cin >> n >> x;

	

	return(0);
}

int main()
{
    _;

	int t = 1; //cin >> t;
	while (t--) {
		solve();
	}
    
    return(0);
}

