#include <bits/stdc++.h>
#define _ ios_base::sync_with_stdio(0);cin.tie(0);
#define endl '\n'
#define all(x) (x).begin(), (x).end()
#define rall(x) (x).rbegin(), (x).rend()
typedef long long ll;
using namespace std;

int solve() {
	int n, p, q, mn = INT_MAX; cin >> n >> p >> q;

	for (int i = 0, x; i < n; i++) {
		cin >> x;
		mn = min(mn, x);
	}
	cout << min(p, q + mn) << endl;

	return(0);
}

int main()
{
    _;

	int t = 1; //cin >> t;
	while (t--) {
		solve();
	}
    
    return(0);
}

