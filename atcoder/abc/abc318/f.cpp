#include <bits/stdc++.h>
#define _ ios_base::sync_with_stdio(0);cin.tie(0);
#define endl '\n'
#define all(x) (x).begin(), (x).end()
#define rall(x) (x).rbegin(), (x).rend()
typedef long long ll;
using namespace std;

/*

entre dois pointos X[i] e X[i + 1] eu tenho a impressao que
eh unimodal poder ou nao poder.

000001111100000000000
e dai seria so somar quantos desses 1s pode ter.
e isso deve dar pra fazer com busca ternaria.

*/

int solve() {

	return(0);
}

int main()
{
    _;

	int t = 1; //cin >> t;
	while (t--) {
		solve();
	}
    
    return(0);
}

