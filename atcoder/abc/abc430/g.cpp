#include <bits/stdc++.h>
#define _ ios_base::sync_with_stdio(0);cin.tie(0);
#define endl '\n'
#define all(x) (x).begin(), (x).end()
#define rall(x) (x).rbegin(), (x).rend()
typedef long long ll;
using namespace std;

/*
	como 1 <= x <= 60

	operacao 1 eh |=  (1 << x) em [l, r]
	operacao 2 eh &= ~(1 << x) em [l, r]

	a query eh max popcount e quantos tem isso
	com segtree nao parece tao simples
*/

int solve() {
	int n, q; cin >> n >> q;

	return(0);
}

int main()
{
    _;

	int t = 1; //cin >> t;
	while (t--) {
		solve();
	}
    
    return(0);
}

