#include <bits/stdc++.h>
using namespace std;

int main()
{
    while (true)
    {
        int x, m;
        cin >> x >> m;
        if (x == 0 && m == 0) break;
        cout << (x*m) << endl;
    }
}
