#include <bits/stdc++.h>
#define f first
#define s second
#define _ ios_base::sync_with_stdio(0);cin.tie(0);
#define endl '\n'
#define dbg(x) cout << #x << " = " << x << endl
typedef long long ll;
const int INF = 0x3f3f3f3f;
const ll LINF = 0x3f3f3f3f3f3f3f3fll;
using namespace std;

// era fundamental entender um lance que chama 'esperanca' da probabilidade. 
// preencher um vetor com a seguinte dp, sendo x[i] a quantidade de pacotes necessarios para conseguir i cartas
// x[i] =   { 0, se i <= 0
//      	{ 1 + sigma(j = A -> B, x[i - j]) / (B - A + 1)

int main()
{
    _;
	

    int n;

    return(0);
}
